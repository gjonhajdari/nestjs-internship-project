<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f8fafc;
    }

    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }

    .header {
      background: linear-gradient(135deg, #33F1A7 0%, #0ea96d 100%);
      padding: 40px 30px;
      text-align: center;
      color: white;
    }

    .logo {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 8px;
      letter-spacing: -0.5px;
    }

    .header-subtitle {
      font-size: 16px;
      opacity: 0.9;
      font-weight: 400;
    }

    .content {
      padding: 50px 40px;
    }

    .icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #33F1A7 0%, #0ea96d 100%);
      border-radius: 50%;
      margin: 0 auto 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 36px;
      color: white;
    }

    h1 {
      font-size: 28px;
      color: #1a202c;
      margin-bottom: 16px;
      text-align: center;
      font-weight: 700;
      line-height: 1.2;
    }

    .description {
      font-size: 16px;
      color: #4a5568;
      text-align: center;
      margin-bottom: 40px;
      line-height: 1.6;
    }

    .name {
      color: #18bb7c;
    }

    .reset-button {
      display: block;
      width: fit-content;
      margin: 0 auto;
      background: linear-gradient(135deg, #33F1A7 0%, #0ea96d 100%);
      color: white;
      text-decoration: none;
      padding: 16px 32px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 16px;
      text-align: center;
      box-shadow: 0 4px 12px rgba(19, 206, 128, 0.3);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .reset-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(6, 182, 212, 0.4);
    }

    .alternative-link {
      margin-top: 30px;
      padding: 20px;
      background-color: #f7fafc;
      border-radius: 8px;
      border-left: 4px solid #06b6d4;
    }

    .alternative-link p {
      font-size: 14px;
      color: #4a5568;
      margin-bottom: 8px;
    }

    .alternative-link a {
      color: #06b6d4;
      word-break: break-all;
      text-decoration: none;
      font-size: 14px;
    }

    .security-notice {
      margin-top: 40px;
      padding: 20px;
      background-color: #fef5e7;
      border-radius: 8px;
      border-left: 4px solid #f6ad55;
    }

    .security-notice p {
      font-size: 14px;
      color: #744210;
      margin: 0;
    }

    .footer {
      background-color: #f7fafc;
      padding: 30px 40px;
      text-align: center;
      border-top: 1px solid #e2e8f0;
    }

    .footer p {
      font-size: 14px;
      color: #718096;
      margin-bottom: 8px;
    }

    .footer a {
      color: #06b6d4;
      text-decoration: none;
    }

    /* Mobile responsiveness */
    @media (max-width: 600px) {
      .email-container {
        margin: 0;
        box-shadow: none;
      }

      .header {
        padding: 30px 20px;
      }

      .content {
        padding: 30px 20px;
      }

      .footer {
        padding: 20px;
      }

      h1 {
        font-size: 24px;
      }

      .reset-button {
        width: 100%;
        padding: 18px 20px;
      }
    }
  </style>
</head>

<body>
  <div class="email-container">
    <!-- Header -->
    <div class="header">
      <div class="logo">Stuck</div>
    </div>

    <!-- Main Content -->
    <div class="content">
      <div class="icon">🔐</div>

      <h1>Reset Your Password</h1>

      <p class="description">
        Hi there <span class="name">{{name}}</span>! We received a request to reset your password. Click the button below to create a new password for your account.
      </p>

      <a href="{{link}}" class="reset-button">Reset Password</a>

      <!-- Alternative link section -->
      <div class="alternative-link">
        <p><strong>Button not working?</strong> Copy and paste this link into your browser:</p>
        <a href="{{link}}">{{link}}</a>
      </div>

      <!-- Security notice -->
      <div class="security-notice">
        <p>
          <strong>Security Notice:</strong> If you didn't request this password reset, please ignore this email. Your
          password will remain unchanged, and no further action is required.
        </p>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>&copy; 2025 Stuck. All rights reserved.</p>
      <p>This link will expire in 24 hours for security reasons.</p>
    </div>
  </div>
</body>

</html>